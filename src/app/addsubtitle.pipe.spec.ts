import { AddsubtitlePipe } from './addsubtitle.pipe';

describe('AddsubtitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AddsubtitlePipe();
    expect(pipe).toBeTruthy();
  });
});
